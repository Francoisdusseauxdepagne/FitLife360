{% extends 'base.html.twig' %}

{% block title %}Messages de Coach{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h2>Messages reçus</h2>
        <ul class="list-group">
            {% for messageCoach in messagesCoach %}
                <li class="list-group-item">
                    <strong>{{ messageCoach.title }}:</strong> {{ messageCoach.content }}
                    <span class="text-muted float-end">{{ messageCoach.createdAt | date('d/m/Y H:i') }}</span>
                </li>
            {% else %}
                <li class="list-group-item">Aucun message pour le moment.</li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
