{% extends 'base.html.twig' %}

{% block title %}Signalement de Commentaire{% endblock %}

{% block meta_description %}Bienvenue sur l'espace de signalement des commentaire de FitLife360.{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="text-center mb-4">Signalement de Commentaire</h1>
                {{ form_start(form) }}
                    <div class="mb-3">
                        {{ form_row(form.comment, {'attr': {'class': 'form-control', 'placeholder': 'Veuillez signaler le commentaire avec le plus de précisions possible'}}) }}
                    </div>
                    <div class="text-center">
                        <button type="submit" class="btn btn-primary">Signaler</button>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
