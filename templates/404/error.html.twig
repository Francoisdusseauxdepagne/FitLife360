{% extends 'base.html.twig' %}

{% block title %}Page non trouvée{% endblock %}

{% block body %}
<div class="container-fuild containerError">
    <div class="container poppins">
        <div class="row justify-content-center">
            <div class="col-lg-12 col-md-12 col-sm-12">
                <div class="text-center">
                    <p class="lead containerError_text colorRed">Oups... la page que vous recherchez n'existe pas.</p>
                </div>
                {# <div class="text-center mt-4 error404">
                    <button class="error404_btn btn btn-lg"><a href="{{ path('app_home') }}" class="text-white text-decoration-none">Retour à l'accueil</a>
                </div> #}
            </div>
        </div>
    </div>
</div>
{% endblock %}
