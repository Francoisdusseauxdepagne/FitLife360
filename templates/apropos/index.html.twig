{% extends 'base.html.twig' %}

{% block title %}FitLife360-À propos{% endblock %}

{% block body %}
<section class="py-5">
    <div class="container text-center">
        <div class="row">
            <div class="col-lg-12 col-md-12 col-sm-12">
                <h1 class="display-4 fw-bold mb-4">FitLife360</h1>
                <p class="lead fs-5">Notre mission est d'inspirer et d'aider les individus à atteindre leurs objectifs de santé et de bien-être en fournissant des ressources et un soutien personnalisé à travers nos coachs de vie expérimentés et nos plateformes en ligne.</p>
            </div>
        </div>
    </div>
</section>
<section class="bg-light py-4">
    <div class="container">
        <h2 class="mb-4">Nos Coachs</h2>
        <div class="row">
            {% for profileCoach in profileCoachs %}
            <div class="col-lg-4 mb-4">
                <div class="card h-100 border-1 shadow-lg p-4">
                    <img src="{{ asset('/images/photos/' ~ profileCoach.photo) }}" class="card-img-top rounded-circle" alt="Photo de profil de {{ profileCoach.firstName }} {{ profileCoach.name }}">
                    <div class="card-body">
                        <h2 class="card-title text-success">{{ profileCoach.firstName }} {{ profileCoach.name }}</h2>
                        <p class="card-text">{{ profileCoach.bio }}</p>
                    </div>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item"><strong>Diplôme:</strong> {{ profileCoach.diplome }}</li>
                        <li class="list-group-item"><strong>Expérience:</strong> {{ profileCoach.experience }}</li>
                        <li class="list-group-item"><strong>Domaines d'expertise:</strong> {{ profileCoach.expertise }}</li>
                    </ul>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</section>
{% endblock %}
