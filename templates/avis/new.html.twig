{# templates/avis/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Ajouter un avis - FitLife360{% endblock %}

{% block body %}
<div class="container-fluid bg-dark py-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-12 col-sm-12">
                <div class="card poppins bg-light">
                    <div class="card-body">
                        <h1 class="card-title playfair">Votre avis nous interesse !</h1>
                        {{ form_start(form, {'attr': {'class': 'form'}}) }}
                        <div class="mb-3">
                            {{ form_label(form.note, 'Note', {'attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.note, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        <div class="mb-3">
                            {{ form_label(form.content, 'Commentaire', {'attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.content, {'attr': {'class': 'form-control', 'rows': 5}}) }}
                        </div>
                        <button type="submit" class="btn btn-primary">🏋️‍♂️ Ajoute ton avis 🏋️‍♂️</button>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
